<!doctype html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title>gua</title>
    <%= javascript_include_tag "jquery", "wScratchPad" %>
    <style type="text/css">
      * { padding: 0; margin: 0;}
    </style>
  </head>

  <body>
    <div class="content-box">
      <div id="wScratchPad" style="display:inline-block; position:relative;"></div>
      <p><%= @tt %></p>
    </div>
    <script type="text/javascript">
<% if @img %>
    var sp = $("#wScratchPad").wScratchPad({
      image: '<%= @img %>',
      image2: '/assets/gua.png',
      width: '190',
      height: '41',
       
      scratchDown: function(e, percent){alert(1)},
      scratchMove: function(e, percent){alert(2);$("#percent_scratched").html(percent)},
      scratchUp: function(e, percent){
        /*   var open_id = "";
              var award_info_id = "";

              $.ajax({
                url: "",
                type: "post",
                dataType: "html",
                data:{ open_id : open_id, award_info_id : award_info_id},
                success:function(data){
                  $(".bbs_dropDown").remove();
                  $(".bbs-a").last().after(data);
                },
                error:function(data){
                  alert("error");
                }
              });
         */
      }

    });
    <% else %>
    $("#wScratchPad").text("活动已过期或者还未开始")
<% end %>
    </script>
  </body>
</html>